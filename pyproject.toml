[project]
name = "dev-setup-cli"
version = "0.1.0"
description = "CLI para setup de ambiente de desenvolvimento no WSL/Linux"
requires-python = ">=3.10"
dependencies = [
  "typer>=0.12.0",
  "rich>=13.0.0"
]

[project.scripts]
devsetup = "devsetup.cli:app"